{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "build",
			"type": "shell",
			"command": "go build -o build"
		},
		{
			"label": "build release",
			"type": "shell",
			"command": "go build -ldflags \"-s -w\" -o build"
		}
	]
}
